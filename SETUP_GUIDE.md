# 📱 Полная инструкция по настройке SafetyAI Mini App

Это пошаговое руководство для развертывания вашего приложения обучения по технике безопасности в Telegram. Все бесплатно!

---

## 🚨 Частые ошибки (читайте перед началом!)

### ❌ ОШИБКА #1: Показывает "Это приложение работает только внутри Telegram"

**Причина:** Скрипт Telegram WebApp не загружается или приложение открыто не в Telegram

**Решение:**
1. ✅ Убедитесь что файл `index.html` содержит:
   ```html
   <script src="https://telegram.org/js/telegram-web-app.js"></script>
   ```
   (Эта строка ОБЯЗАТЕЛЬНА для работы!)

2. ✅ Проверьте что приложение открывается ЧЕРЕЗ TELEGRAM:
   - Нажмите на кнопку Mini App в боте
   - Или перейдите по ссылке: `https://t.me/ваш_бот/ваше_приложение`
   - НЕ открывайте просто в браузере!

3. ✅ URL в BotFather должен быть:
   - HTTPS (не HTTP!)
   - Без параметров (например: `https://your-app.vercel.app`)
   - Vercel автоматически дает HTTPS

4. 🔍 **Как проверить что не так (только Telegram Desktop):**
   - Откройте приложение в Telegram Desktop
   - Нажмите F12 (откроется консоль разработчика)
   - Посмотрите логи - там будет написано что именно не работает
   - Ищите строки начинающиеся с `[SafetyAI]`

### ❌ ОШИБКА #2: "Unused Respond to Webhook node" в n8n

**Причина:** Вы добавили лишнюю ноду "Respond to Webhook"

**Решение:**
1. В n8n удалите ноду "Respond to Webhook"
2. Структура должна быть: **Webhook → OpenAI → Code** (только 3 ноды!)
3. В настройках Webhook оставьте Response Mode = "When Last Node Finishes"
4. НЕ добавляйте отдельную ноду "Respond to Webhook"

### ❌ ОШИБКА #3: Загрузили .env на GitHub

**Это критическая ошибка безопасности!**

**Решение:**
1. НИКОГДА не загружайте файл `.env` на GitHub
2. Загружайте только `.env.example`
3. Убедитесь что `.env` есть в файле `.gitignore`
4. Секретные ключи вводите только в Vercel Environment Variables

### ⚠️ Важно: Правильный путь к данным в n8n

В n8n используйте `{{ $json.body.body.message }}` (да, два раза body!)

---

## 📚 Что мы будем делать?

1. **Загрузим код приложения на GitHub** (бесплатное хранилище кода)
2. **Развернем приложение на Vercel** (бесплатный хостинг для сайтов)
3. **База данных уже готова** (Supabase - уже настроена)
4. **Настроим AI-ассистента через n8n** (бесплатный сервис автоматизации)
5. **Создадим Telegram Mini App** (бесплатно)

---

## Шаг 1: Регистрация на GitHub

**Что такое GitHub?** Это как Google Диск, но для кода. Здесь мы сохраним наше приложение.

### Инструкция:

1. Откройте сайт: https://github.com
2. Нажмите кнопку **"Sign up"** (Зарегистрироваться) в правом верхнем углу
3. Введите:
   - Ваш email (любой)
   - Придумайте пароль (запишите его!)
   - Придумайте имя пользователя (например: `ivanov_safety`)
4. Пройдите проверку (капчу)
5. Подтвердите email - зайдите в почту и нажмите на ссылку от GitHub
6. ✅ Готово! Вы зарегистрированы

---

## Шаг 2: Загрузка кода на GitHub

Теперь загрузим ваше приложение в GitHub.

### ⚠️ КРИТИЧЕСКИ ВАЖНО: Безопасность!

**НИКОГДА не загружайте файл `.env` на GitHub!** В нем хранятся секретные ключи доступа.

Перед загрузкой:
1. Убедитесь, что в проекте есть файл `.gitignore`
2. В `.gitignore` должна быть строка `.env`
3. Это автоматически исключит `.env` из загрузки
4. Вместо этого загрузите файл `.env.example` (шаблон без реальных ключей)

### Вариант A: Через веб-интерфейс (самый простой)

1. Войдите на GitHub (если вышли)
2. Нажмите зеленую кнопку **"New"** в левом меню (или значок "+")
3. Выберите **"New repository"** (Новый репозиторий)
4. Заполните:
   - **Repository name:** `safety-ai-app` (название проекта)
   - **Description:** Telegram Mini App для обучения ТБ
   - Выберите **Public** (публичный) - это нужно для бесплатного хостинга
   - Поставьте галочку **"Add a README file"**
5. Нажмите **"Create repository"**
6. Теперь нажмите **"uploading an existing file"** (или кнопку "Add file")
7. Перетащите все файлы вашего проекта в окно браузера:

   **❌ НЕ загружайте:**
   - Папку `node_modules` - слишком большая, не нужна
   - Папку `dist` - она создастся при сборке
   - Папку `.git` - она создастся автоматически
   - Файл `.env` - **СЕКРЕТНЫЕ КЛЮЧИ!**

   **✅ Обязательно загрузите:**
   - Все файлы `.js`, `.jsx`, `.ts`, `.tsx`
   - Все файлы конфигурации (`package.json`, `vite.config.ts` и т.д.)
   - Файл `.gitignore` - защищает от загрузки секретов
   - Файл `.env.example` - шаблон для других разработчиков
   - Папку `src` со всем кодом
   - Папку `public` с ресурсами

8. Нажмите **"Commit changes"** внизу страницы
9. ✅ Код загружен безопасно!

### Вариант Б: Через Git (если умеете)

```bash
cd /path/to/your/project
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/safety-ai-app.git
git push -u origin main
```

---

## Шаг 3: Регистрация на Vercel

**Что такое Vercel?** Это бесплатный хостинг для веб-приложений. Здесь будет жить ваш сайт.

### Инструкция:

1. Откройте: https://vercel.com
2. Нажмите **"Sign Up"** (Зарегистрироваться)
3. Выберите **"Continue with GitHub"** (Продолжить с GitHub)
4. Разрешите Vercel доступ к GitHub (нажмите "Authorize")
5. ✅ Вы зарегистрированы!

---

## Шаг 4: Развертывание приложения на Vercel

Теперь запустим ваше приложение в интернете.

### Инструкция:

1. На главной странице Vercel нажмите **"Add New..."** → **"Project"**
2. Выберите ваш репозиторий **"safety-ai-app"** из списка
   - Если не видите - нажмите "Adjust GitHub App Permissions" и дайте доступ
3. Нажмите **"Import"**
4. Настройте проект:
   - **Framework Preset:** Vite (должно определиться автоматически)
   - **Build Command:** `npm run build` (уже заполнено)
   - **Output Directory:** `dist` (уже заполнено)
5. Добавьте **Environment Variables** (переменные окружения):

   **Важно:** Здесь мы вводим секретные ключи, которые НЕ загружали на GitHub!

   Нажмите **"Environment Variables"**, добавьте:

   **Первая переменная:**
   - Name: `VITE_SUPABASE_URL`
   - Value: (скопируйте из вашего локального файла `.env`)
   - Например: `https://ваш-проект.supabase.co`

   **Вторая переменная:**
   - Name: `VITE_SUPABASE_ANON_KEY`
   - Value: (скопируйте из вашего локального файла `.env` - это длинный ключ)

   **Третья переменная (добавим позже):**
   - Name: `VITE_N8N_WEBHOOK_URL`
   - Value: (пока оставьте пустым, заполним после настройки n8n)

6. Нажмите **"Deploy"** (Развернуть)
7. Подождите 2-3 минуты - Vercel соберет ваше приложение
8. ✅ Готово! Появится ссылка вида: `https://safety-ai-app.vercel.app`

**Сохраните эту ссылку** - это адрес вашего приложения!

---

## Шаг 5: Настройка AI-ассистента через n8n

**Что такое n8n?** Это инструмент для создания автоматизаций. Мы используем его как мост между вашим приложением и AI.

### Вариант А: n8n Cloud (рекомендуется - проще)

1. Откройте: https://n8n.io
2. Нажмите **"Get started free"**
3. Зарегистрируйтесь через email или Google
4. После входа вы попадете в панель n8n
5. Нажмите **"Add workflow"** (Добавить рабочий процесс)
6. В открывшемся редакторе создайте workflow из **3 нод**:

---

   **Шаг 6.1: Добавьте Webhook триггер**

   - Нажмите на "+" или "Add first step"
   - Найдите **"Webhook"**
   - Выберите его
   - В настройках:
     - **HTTP Method:** POST
     - **Path:** `safety-ai` (или любое другое)
     - **Response Mode:** оставьте "When Last Node Finishes" (по умолчанию)
     - ⚠️ **НЕ выбирайте** "Respond to Webhook" - это важно!
   - Скопируйте **Production URL** - это будет ваш webhook URL!
     - Выглядит как: `https://ваш-аккаунт.app.n8n.cloud/webhook/safety-ai`

---

   **Шаг 6.2: Добавьте OpenAI (или другую AI модель)**

   - Нажмите на "+" **справа** от ноды Webhook
   - Найдите **"OpenAI"**
   - Выберите его
   - В настройках:
     - **Resource:** Message (Chat)
     - **Operation:** Create a Chat Completion
     - **Model:** gpt-3.5-turbo (или gpt-4, если есть доступ)
     - **Messages:** нажмите "Add Message"
       - **Role:** System
       - **Content:** `Ты профессиональный консультант по технике безопасности на производстве. Отвечай на русском языке, будь полезным, точным и конкретным. Давай практические советы.`
     - Нажмите "Add Message" еще раз:
       - **Role:** User
       - **Content:** `{{ $json.body.body.message }}`
         - ⚠️ Важно: используйте `$json.body.body.message` (два раза body!)
     - **Credentials:** нажмите "Create New Credential"
       - Введите ваш OpenAI API Key (см. ниже как получить)
       - Сохраните

---

   **Шаг 6.3: Форматируйте и верните ответ**

   - Нажмите на "+" **справа** от ноды OpenAI
   - Найдите **"Code"** (или "Function")
   - Выберите его
   - В настройках:
     - **Mode:** Run Once for All Items
     - **JavaScript Code:** вставьте этот код:

```javascript
// Получаем ответ от OpenAI
const openAIResponse = items[0].json.choices[0].message.content;

// Возвращаем в нужном формате
return [
  {
    json: {
      response: openAIResponse
    }
  }
];
```

---

   **ВАЖНО:** Структура workflow должна быть:
   ```
   Webhook → OpenAI → Code
   ```

   **НЕ добавляйте ноду "Respond to Webhook"** - она вызывает ошибку!

   Webhook автоматически вернет результат последней ноды (Code).

---

7. Нажмите **"Save"** (Сохранить) вверху справа
8. Дайте имя workflow: "SafetyAI Assistant"
9. Нажмите **"Activate"** (переключатель вверху) - включите workflow
10. Нажмите **"Test workflow"** для проверки
11. ✅ Сохраните **Production URL** - это ваш webhook!

### Получение OpenAI API Key

1. Зайдите на: https://platform.openai.com
2. Зарегистрируйтесь или войдите
3. Перейдите в **API Keys** (слева в меню)
4. Нажмите **"Create new secret key"**
5. Скопируйте ключ (он показывается только один раз!)
6. ⚠️ **Важно:** OpenAI платный, но дают $5 бесплатно для начала

### Альтернатива: Бесплатные AI модели

Если не хотите платить за OpenAI, используйте:

**Вариант 1: HuggingFace (бесплатно)**

1. Зарегистрируйтесь на https://huggingface.co
2. Получите API токен в Settings → Access Tokens
3. В n8n вместо ноды OpenAI используйте **"HTTP Request"**:
   - **Method:** POST
   - **URL:** `https://api-inference.huggingface.co/models/mistralai/Mistral-7B-Instruct-v0.2`
   - **Authentication:** Generic Credential Type
     - **Credential Type:** Header Auth
     - **Name:** Authorization
     - **Value:** `Bearer ваш_токен_huggingface`
   - **Body Content Type:** JSON
   - **Specify Body:** Using JSON
   - **JSON:**
     ```json
     {
       "inputs": "{{ $json.body.body.message }}",
       "parameters": {
         "max_new_tokens": 250,
         "temperature": 0.7
       }
     }
     ```
4. Затем добавьте ноду **"Code"** для форматирования:
   ```javascript
   const response = items[0].json[0].generated_text;
   return [{ json: { response: response } }];
   ```

**Вариант 2: Groq (бесплатно, быстро!)**

1. Зарегистрируйтесь на https://console.groq.com
2. Получите API key (бесплатно!)
3. В n8n используйте **"HTTP Request"**:
   - **Method:** POST
   - **URL:** `https://api.groq.com/openai/v1/chat/completions`
   - **Authentication:** Header Auth
     - **Name:** Authorization
     - **Value:** `Bearer ваш_ключ_groq`
   - **Body:**
     ```json
     {
       "model": "mixtral-8x7b-32768",
       "messages": [
         {
           "role": "system",
           "content": "Ты консультант по технике безопасности. Отвечай на русском."
         },
         {
           "role": "user",
           "content": "{{ $json.body.body.message }}"
         }
       ]
     }
     ```
4. Затем добавьте ноду **"Code"**:
   ```javascript
   const response = items[0].json.choices[0].message.content;
   return [{ json: { response: response } }];
   ```

**Вариант 3: Cohere (бесплатно до 100 запросов/мин)**

1. Зарегистрируйтесь на https://dashboard.cohere.com
2. Получите API key
3. В n8n используйте готовую ноду **"Cohere"** или HTTP Request

---

## Шаг 6: Добавляем Webhook URL в приложение

Теперь соединим AI с вашим приложением.

1. Скопируйте ваш **Production URL** из n8n (из шага 5)
2. Вернитесь в **Vercel**
3. Откройте ваш проект **safety-ai-app**
4. Перейдите в **Settings** → **Environment Variables**
5. Найдите переменную `VITE_N8N_WEBHOOK_URL`
6. Вставьте ваш webhook URL
   - Например: `https://ваш-аккаунт.app.n8n.cloud/webhook/safety-ai`
7. Нажмите **"Save"**
8. Перейдите на вкладку **Deployments**
9. Нажмите на три точки у последнего деплоя → **"Redeploy"**
10. ✅ Готово! AI подключен

---

## Шаг 7: Создание Telegram Mini App

Теперь интегрируем приложение в Telegram.

### Инструкция:

1. Откройте Telegram
2. Найдите бота **@BotFather** (официальный бот для создания ботов)
3. Напишите `/start`
4. Напишите `/newbot`
5. Введите имя бота (например: `SafetyAI Training`)
6. Введите username бота (должен заканчиваться на `bot`):
   - Например: `safety_ai_training_bot`
7. Бот создан! Вы получите токен - **сохраните его**
8. Теперь напишите `/newapp`
9. Выберите своего бота из списка
10. Введите название Mini App: `SafetyAI`
11. Введите описание: `Приложение для обучения технике безопасности с AI-ассистентом`
12. Загрузите иконку (512x512 пикселей) или пропустите
13. Загрузите гифку/видео предварительного просмотра или пропустите
14. **Введите URL вашего приложения:**
    - Вставьте ссылку из Vercel: `https://safety-ai-app.vercel.app`
15. Введите короткое название: `safetyai`
16. ✅ Готово! Mini App создано!

### Тестирование Mini App:

1. В чате с BotFather нажмите на кнопку с вашим приложением
2. Или используйте ссылку: `https://t.me/ваш_бот_username/safetyai`
3. Приложение откроется внутри Telegram!

---

## Шаг 8: Проверка работы

Убедитесь, что все работает:

### ✅ Чек-лист:

- [ ] Приложение открывается в Telegram
- [ ] Видна навигация внизу (5 кнопок)
- [ ] Открывается раздел "Главная" с вашим профилем
- [ ] Открывается раздел "Курсы" со списком курсов
- [ ] Открывается раздел "AI-помощник"
- [ ] AI-ассистент отвечает на вопросы
- [ ] Открывается раздел "Награды"
- [ ] Открывается раздел "Профиль"

### Если что-то не работает:

**Проблема: Ошибка "Unused Respond to Webhook node" в n8n**
- ❌ **Причина:** Вы добавили ноду "Respond to Webhook"
- ✅ **Решение:**
  1. Удалите ноду "Respond to Webhook"
  2. Оставьте только: Webhook → OpenAI → Code
  3. В настройках Webhook убедитесь что Response Mode = "When Last Node Finishes"
  4. Webhook автоматически вернет результат последней ноды

**Проблема: AI не отвечает**
- Проверьте, что добавили `VITE_N8N_WEBHOOK_URL` в Vercel
- Проверьте, что workflow **активен** в n8n (переключатель "Active" включен)
- Проверьте, что в n8n правильно настроен API ключ
- Откройте вкладку "Executions" в n8n - там видны все запросы и ошибки
- Проверьте путь к данным: должно быть `{{ $json.body.body.message }}` (два раза body!)
- Посмотрите в консоли браузера (F12) - там видны запросы к webhook

**Проблема: Ошибка 401 или 403 от AI API**
- Неверный API ключ - проверьте его в настройках ноды
- Для OpenAI: убедитесь что у вас есть кредиты на балансе
- Для Groq/HuggingFace: проверьте что токен скопирован полностью

**Проблема: AI возвращает пустой ответ**
- Проверьте код в ноде Code - правильный путь к ответу
- Для OpenAI: `items[0].json.choices[0].message.content`
- Для HuggingFace: `items[0].json[0].generated_text`
- Посмотрите результат выполнения в n8n - нажмите "Execute Node"

**Проблема: Приложение не загружается**
- Проверьте, что все переменные окружения добавлены в Vercel
- Проверьте логи в Vercel (вкладка Deployments → View Function Logs)
- Убедитесь что deployment успешен (зеленая галочка)

**Проблема: База данных не работает**
- База уже настроена, но если есть проблемы:
- Проверьте `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` в Vercel
- Убедитесь что ключи скопированы полностью без пробелов

**Проблема: "Ошибка подключения" в SafetyAI чате**
- Webhook URL не настроен или неверный
- Проверьте что `VITE_N8N_WEBHOOK_URL` добавлен в Vercel
- После добавления переменной нужно сделать Redeploy в Vercel
- Проверьте что URL начинается с `https://` и заканчивается на ваш path

---

## 🎉 Готово!

Ваше приложение полностью настроено и работает!

### 🔗 Полезные ссылки:

- **Ваше приложение:** `https://safety-ai-app.vercel.app`
- **Telegram Mini App:** `https://t.me/ваш_бот_username/safetyai`
- **GitHub репозиторий:** `https://github.com/ваш-username/safety-ai-app`
- **Vercel панель:** `https://vercel.com/dashboard`
- **n8n панель:** `https://app.n8n.cloud`

### 📊 Что дальше?

1. **Добавьте больше курсов** через Supabase
2. **Настройте AI-ассистента** под ваши нужды
3. **Кастомизируйте дизайн** в коде
4. **Добавьте тесты и сертификаты**
5. **Поделитесь ботом** с коллегами!

---

## 💰 Стоимость всего решения:

- ✅ **GitHub** - Бесплатно навсегда
- ✅ **Vercel** - Бесплатно (100GB трафика/месяц)
- ✅ **Supabase** - Бесплатно (уже настроено)
- ✅ **n8n Cloud** - Бесплатно (5000 исполнений/месяц)
- ⚠️ **OpenAI** - $5 бесплатно, потом $0.002 за 1000 токенов
  - Альтернатива: используйте бесплатные модели (HuggingFace, Cohere)
- ✅ **Telegram** - Бесплатно

**Итого: 100% бесплатно** (или ~$5-10/месяц если используете платный AI)

---

## 🆘 Нужна помощь?

Если что-то не получается:

1. Проверьте каждый шаг еще раз
2. Убедитесь, что все ссылки и ключи скопированы правильно
3. Проверьте логи ошибок в Vercel
4. Проверьте выполнение workflow в n8n (вкладка "Executions")

---

## 📝 Дополнительно: Обновление приложения

Когда вы внесете изменения в код:

1. Загрузите новые файлы на GitHub (как в Шаге 2)
2. Vercel автоматически обновит ваше приложение!
3. Подождите 2-3 минуты
4. Обновите Telegram Mini App (закройте и откройте снова)

---

**Удачи! Теперь у вас есть полноценное приложение для обучения технике безопасности с AI-ассистентом! 🚀**
